import pymongo
import openai
from Tender import Tender
import os 


#os.environ("CHATGRT") use env for local vars
###check here if tender id exist in database to skip one i dont need
int id = 0

def prozorro_traversal():    
    print("traversal from last id in database")
    while(getIdFunc(id))
        tender = Tender(id ,content, price)
        tender.tender_processing()
        id++
    print("prozorro_traveral has finished after scanning the data")